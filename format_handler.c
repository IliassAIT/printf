#include <stdio.h>
/**
 * main - Entry point
 * Return: return (0) success
 */
int main(void)
{

if (format[i] == '\\')
{
	for(j = i, j >= 0; j--)
	{
		if (format[j] != '\\')
			break;
	}
	backslash = (i - j) % 2;
	if (backslash)
	{
		for (j, j < i, j++)
			print_char('\\');
	}
	
//end of backslash handler





if (format[i] == %)
	{
		if (format[i - 1] ==

